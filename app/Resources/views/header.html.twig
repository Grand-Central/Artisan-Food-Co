<header>
<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path('homepage') }}">{{ project_name }}</a>
        </div>
        <div id="navbar" class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                <li{{ (navSelected is defined and navSelected == 'Home')?' class="active"':'' }}><a href="{{ path('homepage') }}">Home</a></li>
                <li{{ (navSelected is defined and navSelected == 'About')?' class="active"':'' }}><a href="#about">About</a></li>
                <li{{ (navSelected is defined and navSelected == 'Contact')?' class="active"':'' }}><a href="#contact">Contact</a></li>
            </ul>
        </div>
    </div>
</nav>
</header>
