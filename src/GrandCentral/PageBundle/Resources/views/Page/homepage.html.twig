
{% extends '::base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="starter-template">
            <h1>Artisan Food Company</h1>
            <p class="lead">Welcome to the Artisan Food Company website.</p>
        </div>
        <div class="row">
            <div class="col-sm-6 col-sm-offset-3">
                {{ render(controller('ApplicationContactFormBundle:ContactForm:form')) }}
            </div>
        </div>
    </div>
{% endblock %}
